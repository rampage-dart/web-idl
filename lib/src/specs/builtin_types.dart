// Copyright (c) 2021 the Rampage Project Authors.
// Please see the AUTHORS file for details. All rights reserved.
// Use of this source code is governed by a zlib license that can be found in
// the LICENSE file.

//------------------------------------------------------------------
// `SingleType`s
//------------------------------------------------------------------

/// The type name of the `any` type.
const String any = 'any';

//------------------------------------------------------------------
// `DistinguishableType`s
//------------------------------------------------------------------

/// The type name of the `sequence` type.
const String sequence = 'sequence';

/// The type name of the `FrozenArray` type.
const String frozenArray = 'FrozenArray';

/// The type name of the `ObservableArray` type.
const String observableArray = 'ObservableArray';

/// The type name of the `object` type.
const String object = 'object';

/// The type name of the `symbol` type.
const String symbol = 'symbol';

//------------------------------------------------------------------
// `PrimitiveType`s
//------------------------------------------------------------------

/// The type name of the `undefined` type.
const String undefined = 'undefined';

/// The type name of the `boolean` type.
const String boolean = 'boolean';

/// The type name of the `byte` type.
const String byte = 'byte';

/// The type name of the `octet` type.
const String octet = 'octet';

/// The type name of the `bigint` type.
const String bigint = 'bigint';

//------------------------------------------------------------------
// `UnrestrictedFloatType`s
//------------------------------------------------------------------

/// The type name modifier for `unrestricted` types.
const String unrestricted = 'unrestricted';

//------------------------------------------------------------------
// `FloatType`s
//------------------------------------------------------------------

/// The type name of the `float` type.
const String float = 'float';

/// The type name of the `double` type.
const String double = 'double';

//------------------------------------------------------------------
// `UnsignedIntegerType`s
//------------------------------------------------------------------

/// The type name modifier for `unsigned` types.
const String unsigned = 'unsigned';

//------------------------------------------------------------------
// `IntegerType`s
//------------------------------------------------------------------

/// The type name of the `short` type.
const String short = 'short';

/// The type name of the `long` type.
const String long = 'long';

//------------------------------------------------------------------
// `StringType`s
//------------------------------------------------------------------

/// The type name of the `ByteString` type.
const String byteString = 'ByteString';

/// The type name of the `DOMString` type.
const String domString = 'DOMString';

/// The type name of the `USVString` type.
const String usvString = 'USVString';

//------------------------------------------------------------------
// `PromiseType`s
//------------------------------------------------------------------

/// The type name of the `Promise` type.
const String promise = 'Promise';

//------------------------------------------------------------------
// `RecordType`s
//------------------------------------------------------------------

/// The type name of the `record` type.
const String record = 'record';

//------------------------------------------------------------------
// `BufferRelatedType`s
//------------------------------------------------------------------

/// The type name of the `ArrayBuffer` type.
const String arrayBuffer = 'ArrayBuffer';

/// The type name of the `DataView` type.
const String dataView = 'DataView';

/// The type name of the `Int8Array` type.
const String int8Array = 'Int8Array';

/// The type name of the `Int16Array` type.
const String int16Array = 'Int16Array';

/// The type name of the `Int32Array` type.
const String int32Array = 'Int32Array';

/// The type name of the `Uint8Array` type.
const String uint8Array = 'Uint8Array';

/// The type name of the `Uint16Array` type.
const String uint16Array = 'Uint16Array';

/// The type name of the `Uint32Array` type.
const String uint32Array = 'Uint32Array';

/// The type name of the `Uint8ClampedArray` type.
const String uint8ClampedArray = 'Uint8ClampedArray';

/// The type name of the `BigInt64Array` type.
const String bigInt64Array = 'BigInt64Array';

/// The type name of the `BigUint64Array` type.
const String bigUint64Array = 'BigUint64Array';

/// The type name of the `Float32Array` type.
const String float32Array = 'Float32Array';

/// The type name of the `Float64Array` type.
const String float64Array = 'Float64Array';
